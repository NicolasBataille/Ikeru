<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- ⚠️ La cible est en GENERATE_INFOPLIST_FILE = NO (comme IkeruWidget),
	     donc CHAQUE clé doit figurer ici : Xcode n'en injecte aucune. Sans
	     CFBundleIdentifier, l'extension se construit et l'app échoue à
	     l'assemblage sur « Embedded binary's bundle identifier is not
	     prefixed with the parent app's bundle identifier » — un message qui
	     accuse le préfixe alors que l'identifiant est simplement absent. -->
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Ikeru</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- Du TEXTE, et rien d'autre. Pas d'images : la reconnaissance a
			     besoin du dictionnaire et d'un temps que le processus d'une
			     extension de partage n'a pas — une photo se passe par l'app,
			     où l'OCR peut échouer proprement et se corriger à la main.

			     Pas de pages web non plus (OBS2-060). La clé
			     NSExtensionActivationSupportsWebURLWithMaxCount a été retirée
			     ici : elle faisait APPARAÎTRE Ikeru dans la feuille de partage
			     de Safari pour une page entière, alors que l'extension n'en
			     retient que `attributedContentText` — c'est-à-dire le TITRE.
			     L'apprenant croyait importer un article et importait une ligne.

			     Extraire le corps de l'article serait l'autre branche, mais
			     elle bute sur exactement la contrainte du paragraphe
			     précédent : ce processus n'a ni le temps ni la mémoire d'un
			     désencombreur de page. Mieux vaut ne pas proposer ce qu'on ne
			     sait pas tenir.

			     Le partage d'une SÉLECTION de texte dans Safari continue de
			     fonctionner : il passe par NSExtensionActivationSupportsText,
			     déclaré séparément juste en dessous. -->
			<key>NSExtensionActivationRule</key>
			<dict>
				<key>NSExtensionActivationSupportsText</key>
				<true/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
