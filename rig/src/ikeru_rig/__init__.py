"""ikeru-rig — Local GPU bridge for Ikeru.

Wraps VOICEVOX (and future heavy models) behind a small FastAPI orchestrator
exposing a job queue REST API for the iPhone client.
"""

__version__ = "0.1.0"
